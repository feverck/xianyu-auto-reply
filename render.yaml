# render.yaml - 闲鱼自动回复系统 Render 部署配置
services:
  - type: web
    name: xianyu-auto-reply
    env: container
    dockerImage: registry.cn-shanghai.aliyuncs.com/zhinian-software/xianyu-auto-reply:latest
    port: 8080
    healthCheckPath: /
    disks:
      - name: xianyu-data
        mountPath: /app/data
        sizeGB: 1
    envVars:
      - key: TZ
        value: Asia/Shanghai
